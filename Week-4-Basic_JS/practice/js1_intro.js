document.getElementById('first').innerHTML = "Through external js script"

// DOM is about manipulating the structure and content of the web pages (HTML/XML).
// BOM provides methods and properties to interact with the browser itself (window, history, navigator).